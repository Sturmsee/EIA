namespace lernquiz{

    //export let htmlQuestion: Question[] = [];

    export let questionsForHTML: string[] = ["What does HTML mean?", ""];

    export let questionsForCSS: string[] = [""];

    export let questionsForTS: string[] = [""];

    export let mixedQuestions: string[][] = [questionsForCSS, questionsForTS, questionsForHTML];
    
}